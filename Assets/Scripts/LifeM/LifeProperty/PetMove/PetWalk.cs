using UnityEngine;
using System.Collections;

public class PetWalk : PetMove {

	// Use this for initialization
	
	public PetGridRun m_run = null;
	public override void Init (Pet pet) {
		base.Init(pet);
	}

	// Update is called once per frame
	public override void Update () {
	
	}
}
